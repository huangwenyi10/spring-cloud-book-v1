#### 应用端口
server:
  port: 8070
#### 应用名称
spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
        #### nacos地址
        server-addr: localhost:8848

#logging:
#  level:
#    com.example.ayuserapi.UserFeignApi: DEBUG
#
#feign:
#  client:
#    config:
#      service-provider:
#        loggerLevel: full


### 全局日志配置
logging:
  level:
    com.example: DEBUG
feign:
  client:
    config:
      ### feign全局日志级别
      default:
        loggerLevel: full
