
server:
  port: 18091

spring:
  application:
    name: spring-cloud-bus

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848


management:
  endpoints:
    web:
    exposure:
      include: '*'
